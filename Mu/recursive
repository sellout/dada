  λ(f : Type → Type)
→   { cata =
          λ(a : Type)
        → λ(algebra : f a → a)
        → λ(fixed : ./Type f)
        → fixed a algebra
    }
  : ./../Recursive/Type
    Type
    ( ./../../dhall-bhat/uncurryT
      Type
      Type
      Type
      ./../../dhall-bhat/Function/Type
    )
    (./Type f)
    f
