  { cata =
        λ(a : Type)
      → λ(alg : ./../algebra Type ./../../dhall-bhat/Function/Type Optional a)
      → λ(fa : Natural)
      → Natural/fold
        fa
        a
        (λ(x : a) → alg ([ x ] : Optional a))
        (alg ([] : Optional a))
  }
: ./../Recursive/Type
  Type
  (./../../dhall-bhat/uncurryT Type Type Type ./../../dhall-bhat/Function/Type)
  Natural
  Optional
