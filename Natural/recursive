  { cata =
        λ(a : Type)
      → λ(alg : ./../algebra Type ./../../caterwaul/Function/Type Optional a)
      → λ(fa : Natural)
      → Natural/fold
        fa
        a
        (λ(x : a) → alg ([ x ] : Optional a))
        (alg ([] : Optional a))
  }
: ./../Recursive/Type Type ./../../caterwaul/Function/Type Natural Optional
