  λ(t : Type)
→ λ(base : Type → Type)
→ λ(cata : ∀(a : Type) → ∀(algebra : base a → a) → ∀(fixed : t) → a)
→ λ(embed : ∀(fa : base t) → t)
→ λ(functor : ./functor  base)
→ λ(fixed : t)
→ cata (base t) (functor.map (base t) t embed) fixed
